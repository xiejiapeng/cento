# 链表思路

## 翻转问题
一般需要一个dummy的头结点
用ListNode a,b维护已经翻转过的范围，同时不断维护这个范围的前和后节点

对于交换链表前后两个部分这种问题，可以对前后两个部分单独进行翻转，然后再整体进行翻转

## 数组与链表
可以把数组当成一种链表使用，数组的位置代表节点，数组的元素值代表这个节点的后继节点。
通过这种思路可以对数组进行原地置换，减少额外的空间复杂度
涉及到整数数组的排序，比如基数排序，也可以使用这个思路，将整个数组看成桶数组。比如L40