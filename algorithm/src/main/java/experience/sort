# 排序题常见思路

## 通过排序，能够使用双指针/滑动窗口
题目中明显的具有两个可变下标，或者有明显的范围的时候，往往考虑采用双指针或者滑动窗口的思路。双指针和原数组的顺序一般关系不大，此时可以考虑对数组进行重新排列。
如果能够对数据进行排序，双指针的移动方向将会有一定的单调性，从而减少时间复杂度。典型的例子为L15,L16,L18这些双指针，三指针，四指针问题。

## 深度优先搜索中使用排序
深度优先搜索之前对元数据进行排序，这样能避免重复答案的出现。